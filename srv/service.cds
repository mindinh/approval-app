// CNMA Approval Service BFF definition (no DB entities)
// Realtime approvals orchestrator matching Conarum guidelines

service ApprovalService @(path: '/api/cnma/APPROVAL_SRV') {
  // Pure passthrough BFF routing
}
